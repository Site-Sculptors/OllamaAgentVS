<UserControl
    x:Class="OllamaAgent.VSIX.OllamaOptionsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:OllamaAgent.VSIX">
    <UserControl.DataContext>
        <local:OllamaOptionsViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="5,2"
                            VerticalAlignment="Center"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                            <!--  Change this value as needed  -->
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel MinWidth="300" Margin="12">

        <TextBlock Text="Ollama Endpoint" />
        <TextBox Text="{Binding Endpoint, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Margin="0,10,0,0" Text="Model" />
        <ComboBox ItemsSource="{Binding Models}" SelectedItem="{Binding SelectedModel}" />

        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <CheckBox
                Grid.Column="0"
                Content="Enable Agent" VerticalAlignment="Center"
                IsChecked="{Binding AgentEnabled}" />

            <Button
                Grid.Column="1"
                Margin="10,0"
                Command="{Binding TestConnectionCommand}"
                Content="Test Connection" />

            <Button
                Grid.Column="2"
                Command="{Binding RefreshModelsCommand}"
                Content="Refresh Models" />

        </Grid>

    </StackPanel>

</UserControl>