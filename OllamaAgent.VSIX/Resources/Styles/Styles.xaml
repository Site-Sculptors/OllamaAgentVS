<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- === CONTROL STYLES === -->
    <!-- Notice: All color references use DynamicResource, NOT StaticResource -->

    <Style x:Key="StyleLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource ColorTextPrimary}"/>
        <Setter Property="Margin" Value="0,8,0,4"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style x:Key="StyleTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource ColorBackgroundInput}"/>
        <Setter Property="Foreground" Value="{DynamicResource ColorTextInput}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ColorBorderInput}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="CaretBrush" Value="{DynamicResource ColorTextPrimary}"/>
    </Style>

    <Style x:Key="StyleComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource ColorBackgroundInput}"/>
        <Setter Property="Foreground" Value="{DynamicResource ColorTextInput}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ColorBorderInput}"/>
    </Style>

    <Style x:Key="StyleCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource ColorTextPrimary}"/>
        <Setter Property="Margin" Value="0,4"/>
    </Style>

    <Style x:Key="StyleButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ColorButtonBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource ColorButtonText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ColorBorderPrimary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ColorButtonHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>